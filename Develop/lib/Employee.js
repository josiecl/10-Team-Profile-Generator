// TODO: Write code to define and export the Employee class
// Employee has name, id, email, then uses functions getName(), getId(), getEmail(), getRole() (employee)
class Employee {

}

module.Exports = Employee;